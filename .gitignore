node_modules
dist
dist-ssr
*.local

# 환경변수 — .env.example 만 커밋한다
.env
.env.*
!.env.example

# 로그
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 에디터
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# MSW 서비스워커(public/mockServiceWorker.js)는 커밋한다.
# 없으면 clone·Codespace 직후 목업이 동작하지 않는다. (msw 공식 권장)

# 코드 전달용 git 번들·패치 (저장소에 커밋하지 않는다)
*.bundle
*.patch
