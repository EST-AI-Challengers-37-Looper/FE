/**
 * 대여 사용 시간 선택지 (분).
 *
 * 서버는 1~10080분(7일)이면 무엇이든 받지만, 화면에서는 캠퍼스 안에서
 * 실제로 일어나는 단위만 고른다. 등록·수정 화면이 같은 목록을 써야
 * 수정할 때 기존 값이 목록에 없어 사라지는 일이 없다.
 */
export const RENTAL_DURATION_OPTIONS = [
  30, 60, 120, 180, 360, 720, 1440, 2880,
] as const;

/** 서버가 허용하는 최대 사용 시간 (7일) */
export const RENTAL_DURATION_MAX_MINUTES = 10_080;
